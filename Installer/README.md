# 📦 AGK ProjectGen - Инструкция по сборке

## 🚀 Как выпустить новую версию

Самый простой способ - использовать скрипт сборки с указанием версии. Это автоматически обновит все файлы проекта и создаст установщик.

```powershell
.\Installer\Build-Installer.ps1 -Version "1.5.0"
```

### Что происходит автоматически:
1. Версия обновляется в `AGK.ProjectGen.UI.csproj`
2. Приложение компилируется и публикуется
3. Inno Setup создает установщик `AGKProjectGen_Settings_v1.1.0.exe`

## 🛠️ Опции скрипта

| Параметр | Описание | Пример |
|----------|----------|--------|
| `-Version` | **(Рекомендуется)** Задает версию сборки | `-Version "1.2.0"` |
| `-Configuration` | Debug или Release (по умолчанию Release) | `-Configuration Debug` |
| `-SkipPublish` | Пропускает пересборку (если только тестируете .iss) | `-SkipPublish` |

## 📁 Ручной способ (если нужно)

1. Измените `<Version>` в `AGK.ProjectGen.UI\AGK.ProjectGen.UI.csproj`
2. Измените `#define MyAppVersion` в `Installer\AGKProjectGen.iss`
3. Запустите `.\Build-Installer.ps1` без параметров
