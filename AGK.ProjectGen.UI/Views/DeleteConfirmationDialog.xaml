<Window x:Class="AGK.ProjectGen.UI.Views.DeleteConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение удаления" 
        Height="500" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="350" MinWidth="450">
    
    <Window.Resources>
        <Style x:Key="WarningHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#DC3545"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="⚠️" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Внимание! Обнаружены файлы" Style="{StaticResource WarningHeaderStyle}"/>
                <TextBlock Text="Следующие папки содержат файлы, которые будут удалены" 
                           Foreground="#64748B" FontSize="13" Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Summary -->
        <Border Grid.Row="1" Background="#FEF2F2" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" Foreground="#991B1B"/>
        </Border>
        
        <!-- Files List -->
        <Border Grid.Row="2" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
            <TreeView x:Name="FoldersTreeView" Background="#F8FAFC" BorderThickness="0" Padding="8">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Files}">
                        <StackPanel Orientation="Horizontal" Margin="2,4">
                            <TextBlock Text="📁" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding FolderName}" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text=" (" Foreground="#64748B"/>
                            <TextBlock Text="{Binding Files.Count}" Foreground="#DC3545" FontWeight="Bold"/>
                            <TextBlock Text=" файлов)" Foreground="#64748B"/>
                        </StackPanel>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2,2">
                                    <TextBlock Text="📄" FontSize="12" Margin="0,0,6,0" Opacity="0.7"/>
                                    <TextBlock Text="{Binding}" Foreground="#64748B" FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </Border>
        
        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Это действие нельзя отменить!" 
                       Foreground="#DC3545" FontStyle="Italic" VerticalAlignment="Center"/>
            
            <Button x:Name="CancelButton" Grid.Column="1" Content="Отмена" 
                    Padding="20,10" Margin="0,0,8,0"
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            
            <Button x:Name="DeleteButton" Grid.Column="2" Content="🗑️  Удалить всё" 
                    Padding="20,10"
                    Background="#DC3545" Foreground="White"
                    Click="DeleteButton_Click">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                        <Setter Property="Background" Value="#DC3545"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
