<UserControl x:Class="AGK.ProjectGen.UI.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:entities="clr-namespace:AGK.ProjectGen.Domain.Entities;assembly=AGK.ProjectGen.Domain"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    
    <UserControl.Resources>
        <!-- Hierarchical Template for Directory Tree -->
        <HierarchicalDataTemplate DataType="{x:Type entities:GeneratedNode}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="2,4">
                <CheckBox IsChecked="{Binding IsIncluded}" Margin="0,0,6,0" VerticalAlignment="Center"
                          ToolTip="Включить/исключить папку из создания"
                          IsEnabled="{Binding Operation, Converter={StaticResource NodeOperationToEnabledConverter}}"/>
                <TextBlock Text="📁" FontSize="16" Margin="0,0,8,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Opacity" Value="0.8"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Operation}" Value="Delete">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="14">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Operation}" Value="Delete">
                                    <Setter Property="Foreground" Value="#888888"/>
                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <!-- NEW метка (зелёная) -->
                <Border Background="{StaticResource AccentBrush}" 
                        CornerRadius="4" 
                        Padding="6,2" 
                        Margin="8,0,0,0"
                        Visibility="{Binding Operation, Converter={StaticResource NodeOperationToVisibilityConverter}}">
                    <TextBlock Text="NEW" FontSize="10" FontWeight="Bold" Foreground="White"/>
                </Border>
                <!-- DELETE метка (красная) -->
                <Border Background="#DC3545" 
                        CornerRadius="4" 
                        Padding="6,2" 
                        Margin="8,0,0,0"
                        Visibility="{Binding Operation, Converter={StaticResource NodeOperationDeleteToVisibilityConverter}}">
                    <TextBlock Text="DELETE" FontSize="10" FontWeight="Bold" Foreground="White"/>
                </Border>
                <!-- ACL иконка -->
                <TextBlock Text="🔐" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center"
                           Visibility="{Binding HasAclRules, Converter={StaticResource BoolToVisibilityConverter}}"
                           ToolTip="Назначены права доступа"/>
            </StackPanel>
        </HierarchicalDataTemplate>
        
        <!-- Modern TreeViewItem Style -->
        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        
        <!-- Tab Header Style -->
        <Style x:Key="WizardTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Padding="16,10" Margin="0,0,4,0" CornerRadius="6,6,0,0"
                                Background="{StaticResource BgSecondaryBrush}">
                            <ContentPresenter ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- ================================ -->
        <!--     HEADER: Profile + Title      -->
        <!-- ================================ -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <TextBlock Text="🚀" FontSize="28" Margin="0,0,16,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding ViewTitle}" Style="{StaticResource H2Style}"/>
                        <TextBlock Text="Заполните данные на каждом шаге" 
                                   Style="{StaticResource LabelStyle}" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Профиль:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Profiles}" 
                              SelectedItem="{Binding SelectedProfile}" 
                              DisplayMemberPath="Name"
                              Style="{StaticResource ModernComboBoxStyle}"
                              MinWidth="220"/>
                </StackPanel>
            </DockPanel>
        </Border>
        
        <!-- ================================ -->
        <!--     WIZARD TABS                  -->
        <!-- ================================ -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0">
            
            <!-- ========== TAB 1: Атрибуты ========== -->
            <TabItem Header="① Атрибуты проекта" Style="{StaticResource WizardTabItemStyle}">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="📝 Атрибуты и параметры" Style="{StaticResource H3Style}" Margin="0,0,0,16"/>
                            
                            <!-- Динамические атрибуты из профиля -->
                            <ItemsControl ItemsSource="{Binding DynamicAttributes}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="300" Margin="0,0,16,16">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                <TextBlock Text="{Binding DisplayName}" Style="{StaticResource LabelStyle}"/>
                                                <TextBlock Text=" *" Foreground="{StaticResource ErrorBrush}" 
                                                           Visibility="{Binding IsRequired, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            </StackPanel>
                                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource ModernTextBoxStyle}"
                                                     ToolTip="{Binding Description}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <!-- Путь создания -->
                            <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,8,0,0">
                                <StackPanel>
                                    <TextBlock Text="📂 Путь создания проекта" Style="{StaticResource H3Style}" Margin="0,0,0,12"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" 
                                                 Text="{Binding ProjectPath, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource ModernTextBoxStyle}"/>
                                        <Button Grid.Column="1" Content="📁 Выбрать" 
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding SelectPathCommand}"
                                                Padding="14,10" Margin="8,0,0,0"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </TabItem>
            
            <!-- ========== TAB 2: Состав ========== -->
            <TabItem Header="② Состав проекта" Style="{StaticResource WizardTabItemStyle}">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="✅ Выберите элементы структуры" Style="{StaticResource H3Style}" Margin="0,0,0,16"/>
                            
                            <!-- LEFT COLUMN: Checkbox Lists (Lists) -->
                            <ItemsControl Grid.Row="1" Grid.Column="0" ItemsSource="{Binding DynamicSelectionGroups}" Margin="0,0,16,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource BgSecondaryBrush}" 
                                                CornerRadius="8" Padding="12" Margin="0,0,0,12">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,8">
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14" DockPanel.Dock="Left" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <Button Content="Все" FontSize="12" Padding="8,4" Margin="0,0,4,0"
                                                                Command="{Binding DataContext.SelectAllInGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource SecondaryButtonStyle}"/>
                                                        <Button Content="Нет" FontSize="12" Padding="8,4"
                                                                Command="{Binding DataContext.DeselectAllInGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource SecondaryButtonStyle}"/>
                                                    </StackPanel>
                                                </DockPanel>
                                                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl ItemsSource="{Binding Items}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <CheckBox IsChecked="{Binding IsSelected}" 
                                                                          VerticalContentAlignment="Center"
                                                                          Margin="0,4" Cursor="Hand">
                                                                    <TextBlock>
                                                                        <Run Text="{Binding Code, Mode=OneWay}" FontWeight="Bold"/>
                                                                        <Run Text=" — "/>
                                                                        <Run Text="{Binding Name, Mode=OneWay}"/>
                                                                    </TextBlock>
                                                                </CheckBox>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </ScrollViewer>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <!-- RIGHT COLUMN: Dynamic Tables (Tables) -->
                            <ItemsControl Grid.Row="1" Grid.Column="1" ItemsSource="{Binding DynamicTableFields}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource BgSecondaryBrush}" 
                                                CornerRadius="8" Padding="16" Margin="0,0,0,16"
                                                HorizontalAlignment="Stretch">
                                            <DockPanel>
                                                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14" DockPanel.Dock="Left" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <Button Content="➕ Добавить" FontSize="12" Padding="10,6" Margin="0,0,4,0"
                                                                Command="{Binding DataContext.AddTableRowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource SecondaryButtonStyle}"/>
                                                        <Button Content="🗑️ Удалить" FontSize="12" Padding="10,6"
                                                                Command="{Binding DataContext.RemoveTableRowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource SecondaryButtonStyle}"/>
                                                    </StackPanel>
                                                </DockPanel>
                                                <DataGrid ItemsSource="{Binding Rows}"
                                                          SelectedItem="{Binding DataContext.SelectedTableRow, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          Style="{StaticResource ModernDataGridStyle}"
                                                          AutoGenerateColumns="False"
                                                          CanUserAddRows="False"
                                                          MinHeight="60"
                                                          MaxHeight="200"
                                                          RowHeight="32">
                                                    <DataGrid.Columns>
                                                        <DataGridTextColumn Header="Код / №" Binding="{Binding Code}" Width="120"/>
                                                        <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                                                    </DataGrid.Columns>
                                                </DataGrid>
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </TabItem>
            
            <!-- ========== TAB 3: Превью ========== -->
            <TabItem Header="③ Превью и создание" Style="{StaticResource WizardTabItemStyle}">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,0">
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🌳" FontSize="20" Margin="0,0,10,0"/>
                                <TextBlock Text="Предварительный просмотр структуры" Style="{StaticResource H3Style}" VerticalAlignment="Center"/>
                                <TextBlock Text="  (снимите ☑ для исключения папки)" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="8,2,0,0"/>
                            </StackPanel>
                            <Button Content="🔍  Сгенерировать превью" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Command="{Binding GeneratePreviewCommand}" 
                                    HorizontalAlignment="Right"
                                    Padding="16,10"/>
                        </DockPanel>
                        
                        <Border Background="{StaticResource BgSecondaryBrush}" 
                                CornerRadius="8" Padding="12"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <TreeView x:Name="PreviewTreeView"
                                      ItemsSource="{Binding PreviewStructure}" 
                                      SelectedItemChanged="PreviewTreeView_SelectedItemChanged"
                                      Background="Transparent" BorderThickness="0" MinHeight="300">
                                <TreeView.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="📂 Открыть в проводнике" 
                                                  Command="{Binding PlacementTarget.DataContext.OpenNodeInExplorerCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext.SelectedNode, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="📋 Копировать путь" 
                                                  Command="{Binding PlacementTarget.DataContext.CopyPathCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext.SelectedNode, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <Separator/>
                                        <MenuItem Header="🔐 Просмотр ACL" 
                                                  Command="{Binding PlacementTarget.DataContext.ViewAclCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext.SelectedNode, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="🔒 Назначить права доступа..." 
                                                  Command="{Binding PlacementTarget.DataContext.AssignAclCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.DataContext.SelectedNode, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <Separator/>
                                        <MenuItem Header="🔄 Пересчитать имена" 
                                                  Command="{Binding PlacementTarget.DataContext.RecalculateNamesCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                    </ContextMenu>
                                </TreeView.ContextMenu>
                            </TreeView>
                        </Border>
                    </DockPanel>
                </Border>
            </TabItem>
        </TabControl>
        
        <!-- ================================ -->
        <!--     FOOTER: Status + Actions     -->
        <!-- ================================ -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,8,0" 
                               Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding StatusMessage}" 
                               Style="{StaticResource BodyStyle}"
                               Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>
                
                <Button Content="{Binding SaveButtonText}" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CreateProjectCommand}" 
                        IsEnabled="{Binding IsPreviewGenerated}"
                        HorizontalAlignment="Right"
                        Padding="28,14"
                        FontSize="15"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
