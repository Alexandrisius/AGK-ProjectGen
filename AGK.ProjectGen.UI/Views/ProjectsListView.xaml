<UserControl x:Class="AGK.ProjectGen.UI.Views.ProjectsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================================ -->
        <!--           📋 HEADER              -->
        <!-- ================================ -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <DockPanel>
                <!-- Title -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Мои проекты" Style="{StaticResource H2Style}"/>
                        <TextBlock Text="Управление созданными проектами · Настройте порядок и ширину столбцов" 
                                   Style="{StaticResource LabelStyle}" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Actions -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="🔄  Обновить" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding LoadProjectsCommand}" 
                            Margin="0,0,8,0"/>
                    <Button Content="➕  Новый проект" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding CreateNewProjectCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ================================ -->
        <!--          📁 PROJECTS LIST        -->
        <!-- ================================ -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <DockPanel>
                <!-- Empty State -->
                <StackPanel Visibility="{Binding ProjectRows.Count, Converter={StaticResource InverseNullToVisibilityConverter}}"
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <TextBlock Text="📂" FontSize="64" HorizontalAlignment="Center" Opacity="0.3"/>
                    <TextBlock Text="Нет созданных проектов" 
                               Style="{StaticResource H3Style}" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" Margin="0,16,0,8"/>
                    <TextBlock Text="Создайте первый проект, чтобы начать работу" 
                               Style="{StaticResource LabelStyle}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
                
                <!-- Projects DataGrid with dynamic columns -->
                <DataGrid x:Name="ProjectsDataGrid"
                          ItemsSource="{Binding ProjectRows}" 
                          SelectedItem="{Binding SelectedProjectRow}" 
                          Style="{StaticResource ModernDataGridStyle}"
                          HeadersVisibility="Column"
                          AutoGenerateColumns="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          ColumnReordered="ProjectsDataGrid_ColumnReordered"
                          ColumnHeaderDragCompleted="ProjectsDataGrid_ColumnReordered">
                    
                    <!-- Двойной клик для открытия проекта -->
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding OpenProjectCommand}"/>
                    </DataGrid.InputBindings>
                    
                    <!-- Столбцы будут добавлены в code-behind -->
                    
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="✏️ Редактировать" Command="{Binding OpenProjectCommand}"/>
                            <MenuItem Header="📂 Открыть папку" Command="{Binding OpenProjectFolderCommand}"/>
                            <Separator/>
                            <MenuItem Header="🗑️ Удалить" Command="{Binding DeleteProjectCommand}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
