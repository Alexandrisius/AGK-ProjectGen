<Window x:Class="AGK.ProjectGen.UI.Views.AclRuleEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:enums="clr-namespace:AGK.ProjectGen.Domain.Enums;assembly=AGK.ProjectGen.Domain"
        Title="🔐 Редактор правил ACL" Height="800" Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimaryBrush}">
    
    <Window.Resources>
        <ObjectDataProvider x:Key="ConditionOperators" MethodName="GetValues" ObjectType="{x:Type enums:ConditionOperator}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:ConditionOperator"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="AccessRights" MethodName="GetValues" ObjectType="{x:Type enums:ProjectAccessRights}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:ProjectAccessRights"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Настройка правил доступа" Style="{StaticResource H2Style}"/>
            <TextBlock Text="{Binding NodeTypeInfo}" Style="{StaticResource LabelStyle}" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Rules List -->
            <Border Grid.Column="0" Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                        <!-- Toolbar с кнопками -->
                        <WrapPanel Orientation="Horizontal">
                            <!-- Правила -->
                            <Button Content="➕" Padding="6,4" Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding AddRuleCommand}" ToolTip="Добавить правило"/>
                            <Button Content="🗑️" Padding="6,4" Margin="2,0,0,0" Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding RemoveRuleCommand}" ToolTip="Удалить правило"/>
                            
                            <!-- Разделитель -->
                            <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="8,4"/>
                            
                            <!-- Пресеты -->
                            <Button Content="📥" Padding="6,4" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding LoadPresetCommand}" 
                                    ToolTip="Загрузить пресет ACL"/>
                            <Button Content="💾" Padding="6,4" Margin="2,0,0,0" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding SaveAsPresetCommand}" 
                                    ToolTip="Сохранить как пресет"/>
                            <Button Content="⚙️" Padding="6,4" Margin="2,0,0,0" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding ManagePresetsCommand}" 
                                    ToolTip="Управление пресетами"/>
                            
                            <!-- Разделитель -->
                            <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="8,4"/>
                            
                            <!-- Группы -->
                            <Button Content="👥" Padding="6,4" 
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding ManageGroupsCommand}" 
                                    ToolTip="Управление группами"/>
                        </WrapPanel>
                    </StackPanel>
                    
                    <ListBox ItemsSource="{Binding Rules}" 
                             SelectedItem="{Binding SelectedRule}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="4">
                                    <TextBlock Text="{Binding PrincipalIdentity}" FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Rights}" FontSize="11" Opacity="0.7"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
            
            <!-- Rule Editor -->
            <Border Grid.Column="1" Background="{StaticResource BgSecondaryBrush}" CornerRadius="8" Padding="16" Margin="8,0,0,0"
                    Visibility="{Binding SelectedRule, Converter={StaticResource NullToVisibilityConverter}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Principal Selection -->
                        <TextBlock Text="Субъект безопасности" Style="{StaticResource H3Style}" Margin="0,0,0,8"/>
                        
                        <TextBlock Text="Выберите из списка:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding AvailablePrincipals}" 
                                  SelectedValue="{Binding SelectedRule.PrincipalIdentity}"
                                  SelectedValuePath="FullName"
                                  DisplayMemberPath="FullName"
                                  Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,8"/>
                        
                        <TextBlock Text="Или введите вручную:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding CustomPrincipalName, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource ModernTextBoxStyle}" 
                                     ToolTip="Формат: DOMAIN\UserName или просто имя"/>
                            <Button Grid.Column="1" Content="Применить" Padding="12,6" Margin="8,0,0,0"
                                    Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding ApplyCustomPrincipalCommand}"/>
                        </Grid>
                        
                        <!-- Rights -->
                        <TextBlock Text="Права доступа" Style="{StaticResource H3Style}" Margin="0,16,0,8"/>
                        <ComboBox ItemsSource="{Binding Source={StaticResource AccessRights}}"
                                  SelectedItem="{Binding SelectedRule.Rights}"
                                  Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,8"/>
                        
                        <CheckBox Content="Запретить (Deny)" IsChecked="{Binding SelectedRule.IsDeny}" Margin="0,0,0,16"/>
                        
                        <!-- Conditions -->
                        <TextBlock Text="Условия применения" Style="{StaticResource H3Style}" Margin="0,0,0,8"/>
                        <TextBlock Text="Правило применяется только если ВСЕ условия выполняются:" 
                                   Style="{StaticResource LabelStyle}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="➕ Условие" Padding="10,6" Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding AddConditionCommand}"/>
                            <Button Content="🗑️ Удалить" Padding="10,6" Margin="8,0,0,0" Style="{StaticResource SecondaryButtonStyle}" 
                                    Command="{Binding RemoveConditionCommand}"/>
                        </StackPanel>
                        
                        <ItemsControl ItemsSource="{Binding CurrentConditions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <ComboBox Grid.Column="0" 
                                                      Text="{Binding AttributePath, UpdateSourceTrigger=PropertyChanged}"
                                                      ItemsSource="{Binding DataContext.CommonAttributes, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      IsEditable="True"
                                                      Style="{StaticResource ModernComboBoxStyle}"/>
                                            
                                            <ComboBox Grid.Column="1" Margin="8,0"
                                                      ItemsSource="{Binding Source={StaticResource ConditionOperators}}"
                                                      SelectedItem="{Binding Operator}"
                                                      Style="{StaticResource ModernComboBoxStyle}" Width="120"/>
                                            
                                            <TextBox Grid.Column="2" 
                                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource ModernTextBoxStyle}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <!-- Description -->
                        <TextBlock Text="Описание правила" Style="{StaticResource LabelStyle}" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding SelectedRule.Description, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource ModernTextBoxStyle}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Placeholder -->
            <TextBlock Grid.Column="1" 
                       Text="Выберите или добавьте правило"
                       Style="{StaticResource LabelStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding SelectedRule, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
        </Grid>
        
        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Padding="20,10" Style="{StaticResource SecondaryButtonStyle}" 
                    IsCancel="True" Click="Cancel_Click"/>
            <Button Content="💾 Сохранить" Padding="20,10" Margin="12,0,0,0" 
                    Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
