<UserControl x:Class="AGK.ProjectGen.UI.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- ================================ -->
        <!--        📑 СПИСОК ПРОФИЛЕЙ        -->
        <!-- ================================ -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,16,0">
            <DockPanel>
                <!-- Header -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="⚙️" FontSize="20" Margin="0,0,8,0"/>
                        <TextBlock Text="Профили" Style="{StaticResource H2Style}"/>
                    </StackPanel>
                    <Button Content="➕  Новый профиль" 
                            Style="{StaticResource PrimaryButtonStyle}" 
                            Command="{Binding CreateProfileCommand}"
                            HorizontalAlignment="Stretch"/>
                </StackPanel>
                
                <!-- Profile List -->
                <ListView ItemsSource="{Binding Profiles}" 
                          SelectedItem="{Binding SelectedProfile}"
                          Background="Transparent"
                          BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd" 
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="6"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgSecondaryBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="14" Margin="0,0,10,0" Opacity="0.7"/>
                                <TextBlock Text="{Binding Name}" FontSize="14"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>
        </Border>
        
        <!-- ================================ -->
        <!--         ✏️ РЕДАКТОР              -->
        <!-- ================================ -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <Grid>
                <!-- Editor (когда выбран профиль) -->
                <DockPanel Visibility="{Binding SelectedProfile, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                    
                    <!-- Header -->
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="✏️" FontSize="24" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="Редактирование профиля" Style="{StaticResource H2Style}"/>
                            <TextBlock Text="Настройка словарей, типов узлов и структуры" 
                                       Style="{StaticResource LabelStyle}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Save Button -->
                    <Border DockPanel.Dock="Bottom" Margin="0,16,0,0">
                        <Button Content="{Binding SaveButtonText}" 
                                Command="{Binding SaveProfileCommand}" 
                                HorizontalAlignment="Left">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSaveSuccess}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Border>
                    
                    <!-- Tabs -->
                    <TabControl Background="Transparent" BorderThickness="0">
                        <TabControl.ItemContainerStyle>
                            <Style TargetType="TabItem">
                                <Setter Property="Padding" Value="16,10"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                            </Style>
                        </TabControl.ItemContainerStyle>
                        
                        <!-- ========== TAB: Общие ========== -->
                        <TabItem Header="📝 Общие">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                                <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                                    <TextBlock Text="Название профиля" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding SelectedProfile.Name, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBoxStyle}"
                                             Margin="0,0,0,16"/>
                                    
                                    <TextBlock Text="Версия" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding SelectedProfile.Version, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBoxStyle}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                        
                        <!-- ========== TAB: Атрибуты ========== -->
                        <TabItem Header="📋 Атрибуты">
                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="280"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Список атрибутов -->
                                <DockPanel Grid.Column="0" Margin="0,0,12,0">
                                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                                        <Button Content="➕ Добавить атрибут" 
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding AddAttributeCommand}" 
                                                Padding="12,8"/>
                                        <Button Content="🗑️ Удалить" 
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Command="{Binding RemoveAttributeCommand}" 
                                                Padding="12,8" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    
                                    <DataGrid ItemsSource="{Binding SelectedProfile.ProjectAttributes}" 
                                              SelectedItem="{Binding SelectedAttribute}"
                                              AutoGenerateColumns="False" 
                                              CanUserAddRows="False"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              GridLinesVisibility="Horizontal"
                                              HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                                              RowHeight="40">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Ключ" Binding="{Binding Key}" Width="120"/>
                                            <DataGridTextColumn Header="Название" Binding="{Binding DisplayName}" Width="*"/>
                                            <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                                            <DataGridCheckBoxColumn Header="Обяз." Binding="{Binding IsRequired}" Width="60"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DockPanel>
                                
                                <!-- Панель свойств атрибута -->
                                <Border Grid.Column="1" 
                                        Background="{StaticResource BgSecondaryBrush}"
                                        CornerRadius="8" Padding="16"
                                        Visibility="{Binding SelectedAttribute, Converter={StaticResource NullToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="Свойства атрибута" Style="{StaticResource H3Style}" Margin="0,0,0,16"/>
                                        
                                        <TextBlock Text="Ключ (ID)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding SelectedAttribute.Key, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,12"/>
                                        
                                        <TextBlock Text="Название" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding SelectedAttribute.DisplayName, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,12"/>
                                        
                                        <TextBlock Text="Тип данных" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <ComboBox SelectedValue="{Binding SelectedAttribute.Type, Converter={StaticResource EnumToStringConverter}}"
                                                  SelectedValuePath="Tag"
                                                  Style="{StaticResource ModernComboBoxStyle}"
                                                  Margin="0,0,0,12">
                                            <ComboBoxItem Content="Строка" Tag="String"/>
                                            <ComboBoxItem Content="Число" Tag="Integer"/>
                                            <ComboBoxItem Content="Да/Нет" Tag="Boolean"/>
                                            <ComboBoxItem Content="Дата" Tag="Date"/>
                                            <ComboBoxItem Content="Список (выбор)" Tag="Select"/>
                                            <ComboBoxItem Content="Множественный выбор" Tag="MultiSelect"/>
                                            <ComboBoxItem Content="Таблица" Tag="Table"/>
                                        </ComboBox>
                                        
                                        <TextBlock Text="Словарь (для списков)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <ComboBox ItemsSource="{Binding SelectedProfile.Dictionaries}"
                                                  DisplayMemberPath="DisplayName"
                                                  SelectedValuePath="Key"
                                                  SelectedValue="{Binding SelectedAttribute.DictionaryKey}"
                                                  Style="{StaticResource ModernComboBoxStyle}"
                                                  Margin="0,0,0,12"/>
                                        
                                        <CheckBox Content="Обязательный" 
                                                  IsChecked="{Binding SelectedAttribute.IsRequired}" 
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="Показывать при создании проекта" 
                                                  IsChecked="{Binding SelectedAttribute.IsProjectAttribute}" 
                                                  Margin="0,0,0,8"/>
                                        <CheckBox Content="Использовать в формулах именования" 
                                                  IsChecked="{Binding SelectedAttribute.CanUseInFormula}" 
                                                  Margin="0,0,0,12"/>
                                        
                                        <TextBlock Text="Значение по умолчанию" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding SelectedAttribute.DefaultValue, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,12"/>
                                        
                                        <TextBlock Text="Описание / подсказка" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                        <TextBox Text="{Binding SelectedAttribute.Description, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource ModernTextBoxStyle}" TextWrapping="Wrap" 
                                                 AcceptsReturn="True" MinHeight="60"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Placeholder -->
                                <TextBlock Grid.Column="1" 
                                           Text="Выберите атрибут для редактирования"
                                           Style="{StaticResource LabelStyle}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="{Binding SelectedAttribute, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                            </Grid>
                        </TabItem>
                        
                        <!-- ========== TAB: Словари ========== -->
                        <TabItem Header="📚 Словари">
                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Список словарей -->
                                <Border Grid.Column="0" Background="{StaticResource BgSecondaryBrush}" 
                                        CornerRadius="8" Padding="8" Margin="0,0,12,0">
                                    <DockPanel>
                                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                            <Button Content="➕" Padding="8,4" Margin="0,0,4,0"
                                                    Command="{Binding AddDictionaryCommand}"
                                                    Style="{StaticResource SecondaryButtonStyle}" ToolTip="Добавить словарь"/>
                                            <Button Content="🗑️" Padding="8,4"
                                                    Command="{Binding RemoveDictionaryCommand}"
                                                    Style="{StaticResource SecondaryButtonStyle}" ToolTip="Удалить словарь"/>
                                        </StackPanel>
                                        <ListBox ItemsSource="{Binding SelectedProfile.Dictionaries}"
                                                 SelectedItem="{Binding SelectedDictionary}"
                                                 Background="Transparent" BorderThickness="0">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DisplayName}" Margin="4"/>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </DockPanel>
                                </Border>
                                
                                <!-- Редактор словаря -->
                                <Border Grid.Column="1" Background="{StaticResource BgSecondaryBrush}" 
                                        CornerRadius="8" Padding="16"
                                        Visibility="{Binding SelectedDictionary, Converter={StaticResource NullToVisibilityConverter}}">
                                    <DockPanel>
                                        <!-- Свойства словаря -->
                                        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                    <TextBlock Text="Ключ (Key)" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding SelectedDictionary.Key, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource ModernTextBoxStyle}"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                    <TextBlock Text="Название" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding SelectedDictionary.DisplayName, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource ModernTextBoxStyle}"/>
                                                </StackPanel>
                                            </Grid>
                                            
                                            <CheckBox Content="Заполняется при создании проекта" 
                                                      IsChecked="{Binding SelectedDictionary.IsDynamic}" 
                                                      Margin="0,12,0,0"
                                                      ToolTip="Если включено — элементы не задаются в профиле, а вводятся при создании проекта"/>
                                        </StackPanel>
                                        
                                        <!-- Элементы словаря (скрыть для динамических) -->
                                        <DockPanel Visibility="{Binding SelectedDictionary.IsDynamic, Converter={StaticResource InverseBoolToVisibilityConverter}, FallbackValue=Visible}">
                                            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                                                <TextBlock Text="Элементы словаря" Style="{StaticResource H3Style}" Margin="0,0,16,0"/>
                                                <Button Content="➕ Добавить" Padding="10,6"
                                                        Command="{Binding AddDictionaryItemCommand}"
                                                        Style="{StaticResource SecondaryButtonStyle}"/>
                                                <Button Content="🗑️ Удалить" Padding="10,6" Margin="8,0,0,0"
                                                        Command="{Binding RemoveDictionaryItemCommand}"
                                                        Style="{StaticResource SecondaryButtonStyle}"/>
                                            </StackPanel>
                                            
                                            <DataGrid ItemsSource="{Binding SelectedDictionary.Items}"
                                                      SelectedItem="{Binding SelectedDictionaryItem}"
                                                      AutoGenerateColumns="False"
                                                      CanUserAddRows="False"
                                                      Background="White"
                                                      GridLinesVisibility="Horizontal"
                                                      HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                                                      RowHeight="36">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="100"/>
                                                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </DockPanel>
                                    </DockPanel>
                                </Border>
                                
                                <!-- Placeholder -->
                                <TextBlock Grid.Column="1" 
                                           Text="Выберите словарь для редактирования"
                                           Style="{StaticResource LabelStyle}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="{Binding SelectedDictionary, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                            </Grid>
                        </TabItem>
                        
                        <!-- ========== TAB: Типы узлов ========== -->
                        <TabItem Header="📦 Типы узлов">
                            <DockPanel Margin="0,16,0,0">
                                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                                    <Button Content="➕ Добавить тип" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding AddNodeTypeCommand}" 
                                            Padding="12,8"/>
                                    <Button Content="🗑️ Удалить" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding RemoveNodeTypeCommand}" 
                                            Padding="12,8" Margin="8,0,0,0"/>
                                </StackPanel>
                                
                                <DataGrid ItemsSource="{Binding SelectedProfile.NodeTypes}" 
                                          AutoGenerateColumns="False" 
                                          CanUserAddRows="False"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          GridLinesVisibility="Horizontal"
                                          HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                                          RowHeight="44">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding TypeId}" Width="*"/>
                                        <DataGridTextColumn Header="Название" Binding="{Binding DisplayName}" Width="*"/>
                                        <DataGridTextColumn Header="Формула имени" Binding="{Binding DefaultFormula}" Width="2*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>
                        </TabItem>
                        
                        <!-- ========== TAB: Структура ========== -->
                        <TabItem Header="🌳 Структура">
                            <Grid Margin="0,16,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Actions -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                    <Button Content="➕ Корень" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding AddRootNodeCommand}" 
                                            Margin="0,0,8,0" Padding="12,8"/>
                                    <Button Content="➕ Дочерний" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding AddChildNodeCommand}" 
                                            Margin="0,0,8,0" Padding="12,8"/>
                                    <Button Content="🗑️ Удалить" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding RemoveNodeCommand}" 
                                            Padding="12,8"/>
                                </StackPanel>
                                
                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="300"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Tree -->
                                    <Border Grid.Column="0" 
                                            Background="{StaticResource BgSecondaryBrush}"
                                            CornerRadius="8" Padding="8"
                                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                        <TreeView ItemsSource="{Binding SelectedProfile.Structure.RootNodes}" 
                                                  SelectedItemChanged="OnStructureNodeSelected"
                                                  Background="Transparent" BorderThickness="0">
                                            <TreeView.ItemTemplate>
                                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                                    <StackPanel Orientation="Horizontal" Margin="4">
                                                        <TextBlock Margin="0,0,8,0" FontSize="14">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Text" Value="📂"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsRoot}" Value="True">
                                                                            <Setter Property="Text" Value="🏠"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <TextBlock Text="{Binding NodeTypeId}" FontWeight="Bold"/>
                                                        <TextBlock Text=" → " Foreground="{StaticResource TextSecondaryBrush}"/>
                                                        <TextBlock Text="{Binding SourceKey}" Foreground="{StaticResource PrimaryBrush}"/>
                                                    </StackPanel>
                                                </HierarchicalDataTemplate>
                                            </TreeView.ItemTemplate>
                                        </TreeView>
                                    </Border>
                                    
                                    <!-- Properties Panel -->
                                    <Border Grid.Column="1" Margin="12,0,0,0"
                                            Visibility="{Binding SelectedStructureNode, Converter={StaticResource NullToVisibilityConverter}}">
                                        <StackPanel>
                                            <TextBlock Text="Свойства узла" Style="{StaticResource H3Style}" Margin="0,0,0,16"/>
                                            
                                            <TextBlock Text="Тип узла" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                            <ComboBox ItemsSource="{Binding SelectedProfile.NodeTypes}" 
                                                      DisplayMemberPath="DisplayName" 
                                                      SelectedValuePath="TypeId" 
                                                      SelectedValue="{Binding SelectedStructureNode.NodeTypeId}"
                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                      Margin="0,0,0,12"/>
                                                      
                                            <TextBlock Text="Кратность" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                            <ComboBox SelectedValue="{Binding SelectedStructureNode.Multiplicity, Converter={StaticResource EnumToStringConverter}}"
                                                      SelectedValuePath="Tag"
                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                      Margin="0,0,0,12"
                                                      ToolTip="Single — одна папка. Dictionary — по словарю (статическому или динамическому).">
                                                <ComboBoxItem Content="Single (одна папка)" Tag="Single"/>
                                                <ComboBoxItem Content="Dictionary (по словарю)" Tag="Dictionary"/>
                                            </ComboBox>
                                            
                                            <TextBlock Text="Источник (словарь)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                            <ComboBox ItemsSource="{Binding SelectedProfile.Dictionaries}"
                                                      DisplayMemberPath="DisplayName"
                                                      SelectedValuePath="Key"
                                                      SelectedValue="{Binding SelectedStructureNode.SourceKey}"
                                                      Style="{StaticResource ModernComboBoxStyle}"
                                                      Margin="0,0,0,12"/>
                                            
                                            <TextBlock Text="Формула имени (override)" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                                            <TextBox Text="{Binding SelectedStructureNode.NamingFormulaOverride}" 
                                                     Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>
                                            
                                            <!-- ACL Button -->
                                            <Button Content="🔐 Настроить ACL" 
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Command="{Binding ConfigureAclCommand}" 
                                                    Padding="12,8"
                                                    ToolTip="Настроить правила доступа для данного узла структуры"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </DockPanel>
            
                <!-- Placeholder (когда профиль не выбран) -->
                <StackPanel Visibility="{Binding SelectedProfile, Converter={StaticResource InverseNullToVisibilityConverter}, FallbackValue=Visible}"
                            HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📋" FontSize="64" HorizontalAlignment="Center" Opacity="0.3"/>
                    <TextBlock Text="Выберите профиль" 
                               Style="{StaticResource H3Style}" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" Margin="0,16,0,8"/>
                    <TextBlock Text="Выберите профиль слева для редактирования словарей и структуры" 
                               Style="{StaticResource LabelStyle}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
